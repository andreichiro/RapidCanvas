# Translation Log

| date | source requirement | interpretation | decision | risk | mitigation | status |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-04-29 | Assignment says "Bluesky Post Explainer" | Bluesky is the required/default social source for this implementation. | Use Bluesky as the selected platform. | Other social sources could be simpler, but would miss the PDF's named task. | Document Bluesky choice in README and requirements matrix. | accepted |
| 2026-04-29 | Assignment includes an API key and asks for `.env` handling | The key must be usable locally but must not be committed. | Provide `.env.example`; keep `.env` ignored. | The key was shared in plain text and may be exposed. | Recommend rotation before real use; add `make check-secrets`. | accepted |
| 2026-04-29 | Evaluation must be thoughtful and reproducible | Cached eval mode is needed in addition to live runs. | Plan cached fixtures as the default eval path. | Live Bluesky/web context can drift. | Add explicit live refresh only in later phases. | accepted |
| 2026-04-29 | DSPy + MLflow requested in plan | MLflow should track evals, providers, GEPA, and packaged DSPy model. | Reserve MLflow commands and dependency extras in T0. | Full MLflow logging is later-phase work. | T11 owns implementation; T0 command fails clearly until then. | accepted |
| 2026-04-29 | Optional providers in bonus | Missing provider keys should not break local setup. | Add optional provider env vars; later `/api/providers` will report configured/skipped. | Evaluator may have only OpenAI key. | OpenAI remains default; optional providers skip gracefully. | accepted |
| 2026-04-29 | GEPA optimization requested | Implementation is required, but full optimization may be cost-controlled. | Reserve `make optimize`; T10 will implement dry-run and real mode. | Premature command could imply fake completion. | Command exits with clear T10 message in T0. | accepted |
| 2026-04-29 | External posts/pages/images can be malicious | Retrieved content must be evidence, not instructions. | Add prompt-injection and guardrail phases. | Model could follow malicious retrieved text. | T14/T15 add sanitization, trust scoring, tool allowlists, and attack tests. | accepted |
| 2026-04-29 | T0 repo safety | Scaffold must be usable without committing generated or secret data. | Add `.gitignore`, `.env.example`, Makefile, README, config module, frontend scaffold, and tests. | Empty repo gives later developers no stable command surface. | Verify with `make lint`, `make test`, and `make check-secrets`. | implemented |
| 2026-04-29 | User requested handoff and deep review workflow before shipping | Review must be repeatable locally and in GitHub. | Add `AGENTS.md`, `docs/deep_review_workflow.md`, `make deep-review`, and `.github/workflows/deep-review.yml`. | Future reviews could miss secret hygiene, generated artifacts, optional deps, or smoke behavior. | Register a single deep review gate and document manual checks. | implemented |
| 2026-04-29 | User requested shipping to `andreichiro/RapidCanvas` | Remote repo did not exist at review time. | Created public GitHub repository and will push the T0 scaffold after review passes. | Wrong visibility or failed auth could block submission. | Used authenticated `gh`; keep repo public for assignment link accessibility. | implemented |
| 2026-04-29 | User requested maintainability/ease-of-change review and user-style testing | Deep review must check code clarity, maintainability, changeability, explainability, unnecessary complexity, and intended user-facing behavior. | Added `scripts/review_quality.py`, `make maintainability-review`, `make user-smoke`, and wired both into `make deep-review`. | Pure lint/tests can miss handoff quality and user-facing regressions. | Automate structural checks and smoke-test backend/frontend surfaces; use browser-use for local UI verification when possible. | implemented |
| 2026-04-29 | User asked to test as a user would where possible | Browser-level verification should confirm the visible scaffold, not only curl/unit tests. | Ran browser-use against `http://127.0.0.1:5174/` and verified title, heading, T0 scaffold label, and plan text. | Automated CI cannot use the in-app browser plugin. | Keep `make user-smoke` in CI and record manual browser-use checks in this log. | implemented |
| 2026-04-29 | Requirement matrix is the Gate 1 closure artifact | Every assignment and Plan Final E requirement needs a concrete mapping to files, tests, eval artifacts, docs, and status. | Added `docs/requirements_matrix.md`, `scripts/check_requirements_matrix.py`, `make requirements-review`, and wired it into `make deep-review`. | Future gates could drift or silently drop a requirement. | CI now fails if required IDs, mappings, or accepted statuses are lost. | implemented |
| 2026-04-29 | User requested a full review of the previous Gate 1 implementation before shipping | The review should be saved, deep, user-style where possible, and tied to repeatable commands. | Added `docs/reviews/gate1_final_review.md` with scope, findings, command results, browser smoke observations, limitations, and ship decision. | Review conclusions could otherwise live only in chat and be lost during handoff. | Commit the review record and rerun `make deep-review` before pushing. | implemented |
| 2026-04-29 | Gate 2 freezes API and domain contracts | The repo needs real Pydantic contracts and routes without fake explanation behavior. | Added `backend/app/schemas/`, `backend/app/api/routes.py`, `backend/app/deps.py`, CORS, provider listing, `/api/explain` URL validation, and contract tests. | Returning fake bullets could misrepresent the product as complete. | `/api/explain` returns `501` until the real Bluesky/search/DSPy pipeline exists; tests verify the success schema separately. | implemented |
| 2026-04-29 | Gate 2 needs review before shipping | API contracts should be reviewed as product-facing surfaces even before the real pipeline exists. | Added `docs/reviews/gate2_final_review.md` with scope, correctness, maintainability, user-style API checks, limitations, and ship decision. | Contract mistakes would multiply across frontend, eval, and agent lanes. | Keep schema tests and OpenAPI checks in `make deep-review`. | implemented |
| 2026-04-29 | User corrected the integration adapter rule | Future gates need real Bluesky fetch, and any temporary Search/RAG or DSPy dev adapter must be explicit and non-final. | Added `R045` to the requirement matrix and documented the adapter rule in README and AGENTS. | Temporary adapters could accidentally be treated as final implementation. | Final acceptance requires real Search/RAG, real DSPy workflow, real citations, real trust/fallback behavior, and real eval; adapter use must be visible in trace. | planned |
| 2026-04-29 | User requested updated handoff before shipping | Future work should start from one concise current-state document rather than chat history. | Added `docs/current_handoff.md` and made maintainability review require it. | Developers could miss current gates, R045, or the no-fake-product-behavior boundary. | README, AGENTS, and deep review checklist now point to the current handoff snapshot. | implemented |
| 2026-04-29 | Gate 3 requires a vertical slice without fake final behavior | Real Bluesky fetch must be active, while unfinished Search/RAG and DSPy behavior may only exist as explicit non-final dev adapters. | Added the real `BlueskyClient`, a trace-marked `Gate3Explainer`, API wiring, frontend submit/result rendering, adapter trace fields, and Gate 3 tests. | Dev adapters could be mistaken for final contextual explanation quality. | Every response marks `adapter_mode=deterministic_dev`, `fallback_mode=safe_summary`, and adapter guardrail flags; R014, R015, and R045 remain planned for final real implementation. | implemented |
| 2026-04-29 | Gate 3 needs user-style verification before shipping | The browser and API should be exercised like a reviewer would use them. | Ran local HTTP against a public Bluesky URL and browser-use against `http://127.0.0.1:5174/`. | Unit tests alone could miss frontend/API integration or invisible adapter markings. | Saved `docs/reviews/gate3_final_review.md` and keep `make deep-review` as the repeatable shipping gate. | implemented |
| 2026-04-29 | Final Gate 3 review found a secret-scan flag issue | The manual secret scan exposed `rg -E` as the wrong flag for regex search. | Changed the Makefile scan to `rg -n -e` and reran the full review gate. | The old command could mask scan errors because stderr was suppressed. | `make check-secrets` and `make deep-review` now exercise the intended regex scan. | implemented |
| 2026-04-29 | Gate 3 review found stale requirement-matrix path | `R008` referenced `scripts/user_smoke_check.py`, but user-style smoke is implemented through Makefile targets and browser-use review notes. | Updated `R008` to point to `Makefile`, frontend/API files, `make user-smoke`, `make deep-review`, and `docs/reviews/gate3_final_review.md`. | A future developer could chase a nonexistent script or distrust the matrix. | Rerun `make requirements-review` and `make deep-review` before shipping the follow-up. | implemented |
| 2026-04-29 | Isolated Lane Protocol for Gate 4 Dev C | The shared `New project` checkout is inspection-only for implementation work. | Created standalone clone `/Users/akatsurada/Documents/rapidcanvas_dev_c_gate4`, branch `codex/dev-c-gate4`, plus lane contract and verifier wrappers. | Implementing from the shared checkout could collide with other lanes. | Ran `scripts/verify_dev_C_gate4_isolation.sh` and `scripts/assert_dev_C_gate4_execution_context.sh` before code edits. | implemented |
| 2026-04-29 | Dev C owns DSPy/guardrails/GEPA/MLflow in Gate 4 | Optional AI/eval dependencies are not installed in the default dev environment. | Added import-safe DSPy signatures/program, trust/output guardrails, GEPA dry-run, MLflow helpers, and command hooks with deterministic unit-test paths. | Deterministic fallback could be mistaken for final live DSPy behavior. | Fallback runner marks `adapter_mode=deterministic_dev`; live DSPy/MLflow paths activate only when extras and credentials are present. | implemented |
| 2026-04-29 | User requested saved work and updated handoff | The Gate 4 Dev C clone needs a current handoff before commit. | Updated `docs/current_handoff.md` with Dev C implementation, verification, and integration boundaries. | Later lanes could assume `/api/explain` already has Dev B Search/RAG. | Handoff states the default route now uses Dev C with trace-marked thread-context evidence until Dev B retrieval is wired. | implemented |
| 2026-04-29 | Dev C deep review findings | The first Dev C pass had API, GEPA, MLflow, signature wiring, and fallback trace gaps. | Routed API through `AgentExplainerService`, added thread-context evidence, real GEPA compile path, in-run MLflow packaging, full signature runner methods, and invalid-shape fallback correction. | Dev B Search/RAG is still absent, and GEPA real runs require provider credentials. | Trace warnings mark thread-context evidence; `--real` GEPA fails clearly without `OPENAI_API_KEY`; tests cover all five review findings. | implemented |
| 2026-04-30 | Dev C follow-up review findings | Installed DSPy GEPA requires a reflection LM, DSPy trust fallback must bind final trace semantics, untrusted labels must be consistent, and Dev C matrix rows must reflect implemented code. | Added GEPA reflection LM configuration, made `dspy_trust_*` flags enforce fallback decisions, added source-type-aware untrusted labels for DSPy evidence, rejected placeholder GEPA keys, and updated Dev C requirement rows. | A fake API key cannot complete live provider calls, and Dev B Search/RAG remains outside this lane. | Tests cover GEPA factory wiring, placeholder-key rejection, DSPy trust abstention, and source labels; handoff states live GEPA needs valid provider credentials. | implemented |
| 2026-04-30 | Dev C third review findings | Live DSPy must receive `.env` credentials, GEPA real mode must not save success after all provider rollouts fail, prompt-injection scans must preserve T15 source labels, and matrix rows must stay honest. | Exported settings-loaded OpenAI keys before `dspy.configure`, added GEPA validation-score failure detection, routed prompt-injection scan inputs through source-specific `UNTRUSTED_*` labels, and updated R025/R026/R036 with exact files and tests. | Real provider calls can still fail with invalid credentials, but those failures should not be recorded as successful optimization. | Added targeted unit tests and live invalid-key verification path; handoff documents the remaining dependency on valid provider credentials and Dev B Search/RAG. | implemented |
| 2026-04-30 | Dev C fourth review findings | Real GEPA output must be a loadable DSPy program, unknown citations must not leave trace fallback as `none`, untrusted source labels must not be spoofable by raw content, and MLflow should package the live DSPy runner path. | Added compiled-program persistence/loading, passed the loaded optimized explain program into `DspySignatureRunner`, forced `unknown_citation` to `partial`, removed label-prefix suppression, and made `make mlflow-log` load the live DSPy runner for packaging. | Real GEPA still requires valid provider credentials and Dev B Search/RAG remains outside Dev C scope. | Added focused tests for compiled persistence/loading, unknown-citation fallback semantics, spoofed-label wrapping, and live-runner packaging; rerun lane guards, focused tests, `make optimize`, `make mlflow-log`, and `make deep-review`. | implemented |
| 2026-04-30 | Dev C fifth review finding | Live DSPy provider/auth errors must not crash `/api/explain`. | Wrapped DSPy predictor calls in a guarded runner fallback, record `dspy_provider_error`, dedupe guardrail penalties, and let trust scoring choose `safe_summary`/`abstain` instead of propagating provider exceptions. | Provider credentials can still be invalid or unavailable, but public API responses must remain schema-valid and visibly downgraded. | Added unit coverage for provider failure degradation and reran the original `OPENAI_API_KEY=sk-test-key` API-path reproduction successfully. | implemented |
| 2026-04-30 | Dev C final ship after Dev A/B/D/E landed | Dev C must merge on top of the shipped mainline without losing other lanes' more complete work. | Fetched GitHub `main`, merged it into the isolated Dev C lane, resolved docs/log/scripts/tests additively, updated README and handoff, and preserved Dev A/B/D/E implementation surfaces. | A stale Dev C branch could delete shipped retrieval, eval, frontend, skills, or handoff work. | Kept the richer mainline generic scripts, retained all lane sections in handoff/logs, updated matrix rows additively, and reran final gates before push/merge. | implemented |
| 2026-04-29 | Isolated Lane Protocol for Dev A Gate 4 | Dev A implementation must not run from the shared `main` checkout or use a linked worktree. | Created a standalone clone at `/Users/akatsurada/Documents/rapidcanvas_dev_a_gate4_isolated`, branch `codex/dev-a-gate4`, with `assets/dev_A_gate4_WORKSPACE_CONTRACT.json` and verifier/context-guard wrappers. | Shared checkout or branch ownership mistakes could overwrite another lane's work. | Ran the lane isolation verifier and execution-context guard before implementation, review, handoff, commit, and push. | implemented |
| 2026-04-29 | Gate 4 Dev A Backend/API/Bluesky scope | Dev A owns Bluesky URL parsing, handle/DID to AT URI construction, real thread fetch normalization, API error mapping, and a read-only Bluesky search wrapper for retrieval lanes. | Expanded `backend/app/clients/bsky.py`, `backend/app/schemas/domain.py`, and API tests for parent/quote/link/image/unavailable handling, sanitized upstream errors, and `ContextDocument` search output. | Real SDK objects can differ from simplified fixtures; raw upstream error bodies could leak into API details. | Added unit/integration coverage, live normalization probes for external/image/quote/record-with-media embeds, and concise upstream error wrapping. | implemented |
| 2026-04-29 | Bluesky `searchPosts` in live unauthenticated mode | The Plan references read-only Bluesky search, but live provider behavior may require auth. | Kept the read-only wrapper and made failures typed/sanitized instead of treating unauthenticated live search as final retrieval completion. | Dev B could assume live Bluesky search always works unauthenticated and fail during retrieval integration. | Documented the `403` live smoke result in `docs/current_handoff.md`; Dev B should handle auth/fallback behavior in the search provider lane. | accepted |
| 2026-04-29 | Cross-lane handoff update requested while acting as Dev A | Handoff docs are usually Dev D-owned, but the user explicitly asked Dev A to update handoff before shipping. | Updated `docs/current_handoff.md` and this log additively without changing requirement-matrix statuses or removing existing plan content. | Cross-lane doc edits could accidentally claim whole-Gate completion or erase planned work. | Preserved Gate 0-3 content, kept Search/RAG/DSPy/eval/frontend rows non-final, and documented remaining lane boundaries. | implemented |
| 2026-04-29 | User assigned Dev B Gate 4 in isolated lane | Dev B must implement retrieval/source-safety scope without modifying the shared repo. | Created standalone clone `/Users/akatsurada/Documents/New-project-dev-b-gate4`, branch `codex/dev-b-gate4-retrieval-safety`, lane contract, verifier, and execution-context guard. | Running implementation from shared `main` could collide with other lanes. | Verified isolation before implementation and before handoff; shared repo stayed on `main`. | implemented |
| 2026-04-29 | Gate 4 Dev B scope | Search, safe fetch, prompt-injection sanitization, embeddings, Qdrant retrieval, and reranking need deterministic tests. | Added Dev B modules under `backend/app/clients/`, `backend/app/guardrails/`, and `backend/app/ml/` plus unit tests. | Optional dependencies could hide behavior differences. | Verified base tests, optional `bluesky` extraction tests, optional `ai` Qdrant test, and `make deep-review`. | implemented |
| 2026-04-29 | Dev B deep review findings | Review found brittle extraction text assertion, dropped RAG prompt-injection diagnostics, and narrow HF fallback. | Updated tests to assert stable content, added `RagService.last_diagnostics`, and broadened HF reranker fallback on model-load failure. | Retrieval safety flags might not reach later agent trace integration. | Added tests for retrieval diagnostics and optional reranker fallback; documented integration note in current handoff. | implemented |
| 2026-04-29 | User asked to save work and update handoff | Handoff docs are normally Dev D-owned, but the user explicitly requested a Dev B handoff update. | Updated `docs/current_handoff.md` in the isolated Dev B clone and logged this cross-lane documentation edit. | Cross-lane doc edits can conflict during merge. | Keep the edit small, factual, and limited to Dev B status/verification. | implemented |
| 2026-04-29 | User said Dev A already shipped before Dev B remote ship | Dev B must not overwrite or lose Dev A work when updating handoff and pushing. | Fetched GitHub remote, compared `github/codex/dev-a-gate4` with Dev B, documented disjoint backend ownership and overlapping docs/scripts, and preserved Dev A integration notes in handoff. | Docs and generic lane scripts can conflict when both branches merge. | Merge docs additively; preserve Dev A `BlueskyClient.search_posts()`/`PostContext.warnings` and Dev B retrieval modules; Dev B context guard keeps stricter checks plus Dev A-style subdirectory allowance. | implemented |
| 2026-04-29 | Merge Dev A and Dev B Gate 4 lanes | Dev A had already landed in `main`; Dev B needed to merge without overwriting better or more complete lane work. | Merged `codex/dev-b-gate4-retrieval-safety` into current `main` after Dev A, resolved docs/log/scripts additively, and preserved both lanes' implementation files and verification notes. | Conflict resolution could accidentally drop Dev A warnings/search wrapper or Dev B retrieval diagnostics. | Compared branches before merge, kept both lane handoff sections, retained stricter lane isolation plus subdirectory execution support, and reran `make deep-review` plus optional Dev B checks. | implemented |
| 2026-04-29 | Dev E Gate 4 requires browser verification of the React UI | The frontend lane should prove the local UI renders result, citations, sources, trust/fallback, guardrail flags, and trace against the local backend. | Ran browser-use at `http://127.0.0.1:5173/` from isolated Dev E clone; verified heading/provider/form, live explain result, 4 citation chips, 2 source cards, `safe_summary`, guardrail flags, open trace, and zero console errors. | Browser verification could be lost because this lane otherwise owns only `frontend/` and README UI sections. | Keep this row as the Dev E verification record and leave deeper docs/matrix changes to Dev D. | implemented |
| 2026-04-29 | Gate 4 frontend is componentized but deep-review still checked only old Gate 3 `App.tsx` scaffold strings | The maintainability review should verify the Dev E component surface directly instead of requiring stale App-only copy. | Updated `scripts/review_quality.py` to check `UrlForm`, `CitationChip`, `SourceList`, `TrustBadge`, `GuardrailFlags`, and `TracePanel` for the required UI surfaces. | Cross-lane review automation edits can surprise Dev D. | Logged this change here and kept the check narrow to frontend surface validation. | implemented |
| 2026-04-29 | Final Dev E review found viewport-based heading font scaling | Frontend design rules prohibit font-size scaling with viewport width. | Replaced the H1 `clamp(..., 4vw, ...)` with rem-only sizes and discrete media-query steps; added `scripts/review_quality.py` coverage for viewport-scaled `font-size`. | The issue passed existing tests and could regress silently. | `make deep-review` now fails if CSS `font-size` uses `clamp()` or viewport units. | implemented |
| 2026-04-29 | User asked Dev E to save, update handoff, and merge after Dev A/B shipped | Dev E must ship without overwriting Dev A/B's better or more complete backend/retrieval work. | Fetched GitHub `main`, merged it into the Dev E lane, resolved docs/log conflicts additively, kept the more capable generic lane scripts from `main`, and preserved Dev E frontend/review-guard work. | Merge conflicts in shared docs/scripts could drop lane-specific handoff details or weaken isolation checks. | Kept Dev A, Dev B, and Dev E sections in `docs/current_handoff.md`; rerun isolation, deep review, frontend build/tests, and optional Dev B checks before push. | implemented |
| 2026-04-29 | Gate 4 review found Dev A/B search-adapter mismatch | Dev A's `BlueskyClient.search_posts()` returns normalized `ContextDocument` objects, but Dev B's `BlueskySearchProvider` was treating every direct-search result as a raw ATProto post. | Added an explicit normalized-document pass-through path that preserves id, title, URL, author, and AT URI while still sanitizing and annotating rank/query metadata. | Integration would otherwise collapse citations to `https://bsky.app` with `author=unknown` when Dev B wraps Dev A's client. | Added `test_bluesky_search_provider_preserves_normalized_context_documents` and reran targeted lint, mypy, tests, and the reproduction probe. | implemented |
| 2026-04-29 | Third Gate 4 review found Dev B source-safety and reranker edge cases | The prompt-injection scanner missed the plan's exact `ignore all instructions` attack phrase, and the optional cross-encoder reranker collapsed negative logits before sorting. | Expanded the instruction-override regex to catch `ignore instructions`/`ignore all instructions`; changed cross-encoder reranking to sort on raw logits. | Attack fixtures could pass unflagged, and HF reranking could silently keep vector order when all logits are negative. | Added regression tests for the exact attack wording and negative-logit ordering, then reran targeted checks and manual probes. | implemented |
| 2026-04-30 | User requested real-key behavior checks before shipping review fixes | Earlier review used real Bluesky/browser/API behavior but did not exercise the provided OpenAI key. | Used the key transiently through process environment only to test provider detection, real OpenAI embeddings, RagService retrieval with in-memory and Qdrant vector stores, real Bluesky fetch, and `/api/explain` safe-summary response. | The key could accidentally be committed or the dev-adapter path could be mistaken for final real DSPy/Search implementation. | Ran `make check-secrets`; documented that Search/RAG/DSPy remain trace-marked dev adapters in `/api/explain` until later integration, while the OpenAI embedding and vector retrieval modules were real-tested. | implemented |
| 2026-04-29 | Isolated Lane Protocol for Gate 4 Dev D | The shared `/Users/akatsurada/Documents/New project` checkout is inspection-only for this lane. | Created standalone clone `/Users/akatsurada/Documents/rapidcanvas_dev_d_gate4`, branch `codex/dev-d-gate4-eval-docs`, lane contract, verifier, and execution-context guard. | Running implementation from the shared checkout could collide with other lanes. | Verified isolation and context before product/eval edits. | implemented |
| 2026-04-29 | Dev D owns eval/docs/skills in Gate 4 | Eval must run before the final real agent exists, but fixtures cannot masquerade as product behavior. | Added cached eval cases, deterministic metrics, report generation, research docs, task packets, and local skills. | Fixture-backed predictions could be mistaken for final Search/RAG or DSPy. | `make eval` is documented as offline eval only; product adapters remain marked non-final in trace and matrix rows. | implemented |
| 2026-04-29 | `eval/posts.yaml` requested while PyYAML is not a base dependency | Default eval should avoid adding parser requirements to the dev test path. | Stored JSON-compatible YAML and parse it with the standard-library JSON loader. | Humans may expect block-style YAML. | Documented the choice in `docs/research/testing_eval.md`; future live refresh may add PyYAML if needed. | accepted |
| 2026-04-29 | Dev D needed to expose `make eval` | The Makefile and review automation are shared workflow files, but the eval command belongs to the Dev D deliverable. | Updated `make eval` and the maintainability review expectation. | Cross-lane workflow edit could surprise other developers. | Logged this entry and kept the command offline, deterministic, and generated-artifact-safe. | implemented |
| 2026-04-29 | Gate 4 Dev D validation | Handoff should record that skills and eval reports were actually exercised. | Ran four skill validations, `make deep-review`, and `make eval` in the isolated clone. | Generated reports or dependency folders could be mistaken for tracked artifacts. | `.gitignore` ignores `reports/eval`, `.venv`, and `node_modules`; `make clean-generated` removes eval reports before maintainability checks. | implemented |
| 2026-04-29 | Review found Dev D eval gaps | Cached eval existed, but runner and judge code did not expose fake/API agent modes or selectable Ragas/DSPy judge backends. | Added `EvalAgent` adapters, `--mode cached\|fake-agent\|api`, `--judge deterministic\|dspy\|ragas\|composite`, injected judge tests, and broader metric tests. | Default eval could become non-reproducible if live judges ran implicitly. | Kept `make eval` deterministic/offline and documented optional live/model-backed modes explicitly. | implemented |
| 2026-04-29 | API eval mode crashed on synthetic eval URLs | The current eval dataset intentionally uses cached synthetic Bluesky URLs, while the Gate 3 API attempts real Bluesky fetches. | Changed API eval failures into scored rows with `api_eval_error` instead of aborting the run. | Real-agent eval could stop before producing reports whenever one post is unavailable. | Added unit coverage for HTTP failure handling and smoked `--mode api --judge deterministic`. | implemented |
| 2026-04-29 | Review found optional DSPy/Ragas judge modes were not runnable | `--judge dspy` lacked LM configuration and `--judge ragas` expected an implicit OpenAI key. | Configured DSPy from `OPENAI_API_KEY`/`dspy_judge_model` when available, added a no-network DSPy `BaseLM` for local review, made Ragas call `ragas.evaluate` with OpenAI-backed LLM metrics when configured and non-LLM context metrics without a key, and added `rapidfuzz` for Ragas string metrics. | Optional judge commands could fail during review or make surprise live calls. | Default `make eval` remains deterministic; optional commands now state their extras and no-key behavior, and targeted smoke commands verify both modes. | implemented |
| 2026-04-30 | Review found project skill validation was documented but missing | Dev D handoff claimed skill `quick_validate.py` had run, but no validator existed under skills or scripts. | Added `scripts/quick_validate.py`, skill-local `quick_validate.py` wrappers for all four skills, `make skills-review`, and wired skill validation into `make deep-review`. | Future handoff could again claim unverified skills if validation stayed manual. | `make skills-review`, each skill-local validator, and `make deep-review` now fail when skill metadata, agent config, or references are missing. | implemented |
| 2026-04-30 | Third review found API eval/reporting gaps | API mode handled HTTP status errors but not route exceptions or non-JSON bodies, non-cached reports were labeled as cached/no-network, and DSPy judge row metrics were not summarized. | Converted all API per-case failures into `api_eval_error` abstain fixtures, added run metadata for prediction mode/judge/cached-live counts/API-model allowance, and aggregated dynamic numeric judge metrics into summaries and report artifacts. | Real-agent eval could abort or mislead reviewers about live/model calls; DSPy judge metrics could be hidden in JSONL only. | Added regression tests for API exceptions, non-JSON errors, API report metadata, model judge metadata, and dynamic DSPy judge aggregation; provider-backed full 18-case DSPy/Ragas smoke uses only ignored local `.env`. | implemented |
| 2026-04-30 | Merge Dev D after Dev A/B/E shipped | Dev D needed to land eval/docs/skills without overwriting already-shipped backend, retrieval, frontend, handoff, or review-gate improvements. | Fetched `origin/main`, merged it into the Dev D lane, kept the more capable generic lane scripts from `main`, preserved Dev A/B/E implementation files, and resolved docs/logs additively with a Dev D lane section. | A naive old-base merge would delete Dev A/B/E files or weaken shared review scripts. | Compared branch diffs, used `origin/main` for non-Dev-D lane scripts/content where more developed, added Dev D status additively, and reran final gates before push. | implemented |
| 2026-04-30 | User clarified Gate 5 and Gate 6 parallelization | Gates 5, 6, and 7 remain sequential relative to each other, but each gate can be parallelized internally by developer lane. | Added `docs/gate5_parallelization_plan.md` with Dev A/B/C/D/E ownership, must-not-edit boundaries, integration spine, merge order, and final review criteria; linked it from `docs/current_handoff.md`. | Without this detail, future work could either serialize too much and lose productivity or parallelize gate dependencies and measure unfinished adapters. | Gate 5 uses parallel branches plus one serial end-to-end integration review; Gate 6 starts only after Gate 5 lands and may then parallelize internally. | accepted |
| 2026-04-30 | User requested the same detailed internal parallelization for Gate 6 | Gate 6 must be sequentially after Gate 5, but its quality-layer work should run in parallel by developer lane. | Added `docs/gate6_parallelization_plan.md` with Dev A/B/C/D/E ownership, must-not-edit boundaries, quality spine, merge order, and final review criteria; linked it from `docs/current_handoff.md`. | Gate 6 could otherwise either block on one eval owner or let metrics measure the old dev adapter instead of the real Gate 5 pipeline. | Gate 6 starts only after Gate 5 lands; each lane works in parallel on API eval smoke, retrieval diagnostics, agent/guardrail/MLflow hooks, eval reports, and frontend quality verification. | accepted |
| 2026-04-30 | Review found clean checkout `make setup && make deep-review` failed without DSPy extras | The registered review gate now exercises DSPy/MLflow/eval code, so documented setup must install the dependencies required by that gate. | Changed default `make setup`/`setup-backend` to install all backend extras plus dev dependencies; kept `setup-backend-full` as an alias. | Setup is heavier, but skipping or weakening DSPy tests would make the review gate less honest. | CI and local reviewers now use the same full dependency path before deep review. | implemented |
| 2026-04-30 | Review found `make optimize` dirtied a tracked artifact on every dry run | Dry-run optimization is a verification command and should be repeatable without timestamp-only churn. | Made dry-run `saved_at` deterministic, added write-if-changed persistence, updated the tracked dry-run artifact, and added idempotency coverage. | Real optimization still needs timestamped deliberate updates. | Real mode keeps live timestamps; dry-run verification remains clean after the committed deterministic artifact is updated once. | implemented |
| 2026-04-30 | Review found eval URLs were synthetic but requirement status implied final real Bluesky-post coverage | The current eval cases are valuable deterministic fixtures, but they are not fetchable public Bluesky posts and cannot close the final 10+ public-post requirement. | Marked R017/R018 as planned, documented the synthetic fixture boundary in README, handoff, and testing research, and left Gate 6 responsible for real/fixture-backed public post cases. | Reviewers could otherwise overestimate final eval readiness. | Matrix now stays honest while preserving the synthetic attack and workflow fixtures for reproducible Gate 4 review. | implemented |
| 2026-04-30 | Review found handoff described Dev C as unmerged after Dev C/D landed on main | Gate 5 teams need an accurate baseline before starting parallel integration work. | Updated `docs/current_handoff.md` and README status to show Dev A/B/C/D/E merged into `main` and point next work to Gate 5/6 plans. | Stale branch language could send developers to the wrong baseline. | Current handoff now names `main` as the baseline and links the Gate 5 and Gate 6 parallelization plans. | implemented |
| 2026-04-30 | User found Gate 5 mini-spine checks confusing for isolated lanes | The plan needs explicit cross-lane handoff checkpoints that do not break independent lane ownership. | Replaced vague mini-spine language with C0-C5 contract checkpoints in `docs/gate5_parallelization_plan.md`, including producers, consumers, artifacts/tests, and pass conditions. | Developers could otherwise either wait too long to integrate or edit another lane directly. | Checkpoints use fixtures/protocols and Dev D-owned shared artifacts; only final C5 closes real-pipeline requirement rows. | accepted |
| 2026-04-30 | Isolated Lane Protocol for Dev A Gate 5 | Dev A C0/C1 implementation must run outside the shared `main` checkout. | Created standalone clone `/Users/akatsurada/Documents/rapidcanvas_dev_a_gate5_isolated`, branch `codex/dev-a-gate5-api-bluesky`, plus Gate 5 lane contract and wrapper scripts. | Shared-root implementation could collide with other lanes or switch the wrong checkout. | Ran the generic lane verifier and execution-context guard before implementation; shared repo stayed read-only on `main`. | implemented |
| 2026-04-30 | Dev A Gate 5 checkpoint responsibility | Dev A should satisfy C0/C1 without waiting for or implementing Dev B/C/D/E. | Kept public API shape stable, enriched `PostContext` for Dev B, added a cached C1 PostContext fixture test, and propagated Bluesky warnings into trace via Dev A-owned dependency wiring. | Schema additions could be mistaken for final Gate 5 Search/RAG or DSPy completion. | Existing public response shape is unchanged, adapters remain trace-marked, and matrix rows for Search/RAG, DSPy, final citations, trust/fallback, and eval stay unclaimed. | implemented |
| 2026-04-30 | Cross-lane handoff note requested while acting as Dev A Gate 5 | Handoff docs are Dev D-owned, but the user explicitly required a Dev A handoff note and translation-log record before handoff. | Updated `docs/current_handoff.md` and this log additively with Dev A C0/C1 changes, Dev B consumption guidance, live Bluesky limits, and remaining integration work. | Cross-lane docs could accidentally claim whole-Gate completion or conflict with Dev D. | Kept the note factual, C0/C1-scoped, and did not change requirement-matrix statuses. | implemented |
| 2026-04-30 | Dev A Gate 5 review findings | Warning propagation must be request-local, and runtime/test additions must stay within explicit Dev A ownership. | Replaced shared warning state with `ContextVar` request context, added a concurrency regression test, removed extra Bluesky helper modules and the extra unit test file, and kept normalization/search coverage in owned files. | A subtle API trace race or ownership drift could block C0/C1 handoff. | Focused API/Bluesky tests, mypy, lint, and deep-review cover the fixes; handoff documents the review follow-ups. | implemented |
| 2026-04-30 | Dev A Gate 5 quoted-post review finding | The C1 fixture had nested quoted-post `record.value.created_at` and `cid` that were present in input but missing from normalized `PostContext`. | Routed quoted-post `created_at` and `cid` through the nested record-view helper and asserted both fields in the C1 handoff fixture test. | Dev B could receive incomplete quoted-post context from C1 even though the fixture contained the metadata. | Focused API/Bluesky tests and direct fixture probes now verify quoted post timestamp and CID preservation. | implemented |
| 2026-05-01 | Dev A Gate 5 C3/C5 route-layer wiring after Dev C handoff | Dev C exposed `build_agent_explainer_service(...)`, and Dev B PR #3 exposed `build_retrieval_service(...)`; Dev A must wire only API/dependency composition. | Fetched `github/dev-c-gate4` and PR #3, added lazy route-layer composition in `backend/app/deps.py`, passed planned Dev C queries through a sync adapter to Dev B retrieval, and covered the contract in API integration tests. | Eager imports would break the isolated Dev A branch before PRs are combined, while a non-query-aware adapter would drop Dev C query planning. | Lazy imports keep C0/C1 review green until C5 merge; once both builders exist, `/api/explain` composes the real fetch/retrieval/explainer path without reimplementing DSPy or retrieval logic. | implemented |
| 2026-05-01 | Dev A publish and C5 coordination handoff | Dev A must publish C1/C5 route-layer work while keeping Dev E and Dev D aware of downstream responsibilities. | Updated the handoff to state that Dev A only wires API/dependency composition, final `/api/explain` becomes real only when Dev B and Dev C builders are present together, Dev E should check UI impact only if public response/trace changes, and Dev D owns final review/matrix/docs/eval bookkeeping. | A Dev A PR could otherwise be mistaken for a complete Gate 5 product path or leave Dev D/E out of the integration loop. | The publish PR remains Dev A-scoped; C5 integration must combine Dev A, Dev B PR #3, and Dev C PR #4 before rerunning final checks. | implemented |
| 2026-04-30 | Isolated Lane Protocol for Gate 5 Dev B | Dev B Gate 5 implementation must run in a standalone clone, not the shared `/Users/akatsurada/Documents/New project` checkout. | Created `/Users/akatsurada/Documents/rapidcanvas_dev_b_gate5`, branch `codex/dev-b-gate5-retrieval-safety`, plus `assets/dev_B_gate5_WORKSPACE_CONTRACT.json` and verifier/context wrappers. | Running live-lane edits from the shared checkout could collide with other lanes or app-level git actions. | Ran `scripts/verify_dev_B_gate5_isolation.sh` and `scripts/assert_dev_B_gate5_execution_context.sh` before implementation. | implemented |
| 2026-04-30 | Gate 5 Dev B C2 checkpoint | Dev B needs a typed retrieval service that turns Dev A `PostContext` plus queries into evidence and diagnostics without wiring the public route. | Added `backend/app/ml/retrieval_service.py`, richer `RetrievalDiagnostics`, explicit blocked search URL warnings, C2 fixtures, and focused retrieval tests. | Checkpoint output could be mistaken for final end-to-end Search/RAG acceptance. | Handoff states C2 is a lane checkpoint only; requirements matrix rows stay unchanged until C5 final integration. | implemented |
| 2026-04-30 | Gate 5 Dev B live-service limits | Production retrieval should not use deterministic embeddings, but local review must still be reproducible and safe without live credentials. | Default builder uses OpenAI embeddings and Qdrant local mode; tests inject fake embeddings and in-memory vectors; Qdrant creation failures emit an explicit fallback warning. | Missing `OPENAI_API_KEY`, DDGS/network drift, or unauthenticated Bluesky search can limit live retrieval. | Retrieval surfaces provider/fetch/vector warnings, private URL block evidence, and guardrail flags for Dev C trace/trust and Dev D metrics. | implemented |
| 2026-04-30 | Gate 5 Dev B review findings | The C2 handoff fixture must be generated from coherent retrieval output, and controlled/offline callers must be able to disable live search providers explicitly. | Regenerated and locked the C2 fixture against deterministic retrieval output, preserved prompt-injection warning references to included documents, and changed `build_retrieval_service(search_providers=[])` to keep an explicit empty provider list. | Dev C could otherwise consume stale fixture references or accidentally trigger live Bluesky/DDGS search during an offline handoff run. | Added integration coverage that compares the fixture to service output and verifies explicit empty search providers are not replaced by defaults. | implemented |
| 2026-04-30 | Gate 5 Dev B source-safety review findings | Malformed URLs and redirect locations must degrade into blocked evidence, prompt-injection scans must expose the generic Dev C/eval flag, and per-call retrieval settings must not ignore provider-specific search toggles. | Block malformed URLs in URL safety validation and redirect handling, add `prompt_injection_risk` to retrieval guardrail flags when scanner flags exist, and filter installed Bluesky/web providers for each retrieval call. | Untrusted links/redirects could crash retrieval, Dev C trust scoring could miss prompt-injection evidence, or controlled handoff runs could make unintended live search calls. | Added focused fetcher and Gate 5 integration regressions, updated the C2 fixture, and documented the stable behavior in handoff. | implemented |
| 2026-04-30 | Gate 5 Dev B web-search redirect block evidence | A DDGS result can pass initial URL validation and then redirect to a private, local, or malformed URL. | When `LinkedPageFetcher` blocks a fetched search hit, `WebSearchProvider` now records `blocked_url:<original-url>:<warnings>` so retrieval promotes it into `private_url_blocks`. | Without a `blocked_url:` wrapper, Dev C and Dev D would see only raw fetch warnings and miss `private_url_blocked` diagnostics. | Added unit coverage for redirect-blocked web-search hits and integration coverage for `RetrievalResult.private_url_blocks`. | implemented |
| 2026-04-30 | Gate 5 Dev B search/fetch failure isolation | Search providers and linked-page extraction handle untrusted/live failures inside retrieval diagnostics. | Built-in providers now return per-call `SearchBundle` warnings, `collect_search_context()` catches provider exceptions as `search_provider_failed:*`, and extraction/parser exceptions return `extraction_failed:*` fetch warnings. | Shared provider instances could leak warnings between concurrent retrievals, one provider could abort C2 retrieval, or malformed pages could crash linked-page fetch. | Added concurrent warning-isolation, provider-exception, and extraction-failure regression tests plus manual probes. | implemented |
| 2026-04-30 | Gate 5 Dev B untrusted search metadata | DDGS result titles and snippets are untrusted evidence even when the fetched page body is safe. | Sanitize search hit title/snippet metadata, fold them into the scanned untrusted document text, and surface prompt-injection warnings from both fetched-page and fallback search documents. | Dev C could otherwise receive attack text in titles/snippets with no guardrail flag. | Added a web-search regression with malicious title/snippet and reran focused plus deep-review checks. | implemented |
| 2026-04-30 | Gate 5 Dev B final source-safety hardening | Follow-up review found title-only prompt injection, credential-bearing URLs, DNS rebinding risk, and unsafe normalized document URLs. | Scan sanitized document titles with body text, reject URL userinfo with redacted diagnostics, validate the connected peer before accepting bytes, and filter unsafe source URLs from normalized/custom documents into block evidence. | Dev C could otherwise consume unflagged malicious titles, outbound fetches could carry Authorization headers, a rebinding host could bypass preflight DNS checks, or private source URLs could enter citations. | Added unit/integration regressions for title scanning, userinfo blocking/redaction, peer-address blocking, and unsafe provider document URL blocks; handoff documents the stable behavior. | implemented |
| 2026-04-30 | Gate 5 Dev B reranker runtime hardening | Third-pass code review found optional HF reranking could still abort retrieval if the loaded model failed during prediction. | `CrossEncoderReranker` now falls back to similarity ordering on prediction/shape failures while preserving raw cross-encoder ordering when prediction succeeds. | A transient HF runtime failure could otherwise produce no C2 evidence despite valid vector candidates. | Added a regression for prediction-time fallback and reran focused Dev B checks. | implemented |
| 2026-04-30 | Gate 5 Dev B final review hardening | Fourth-pass review found malformed provider bundle results and whitespace/control-character URLs could still escape as crashes instead of diagnostics. | Moved search-bundle consumption inside provider exception handling, blocked whitespace/control characters during URL parsing, and stripped query/fragment data from blocked URL diagnostics. | A malformed handoff provider or adversarial URL path could otherwise abort retrieval or echo token-like URL data. | Added provider-shape and malformed URL regressions and reran focused checks before the final full review. | implemented |
| 2026-04-30 | Gate 5 Dev B final pass-through review | Fresh source-metadata review found DID-style `at://` URIs could be misclassified as malformed host:port URLs before the Bluesky/thread allowlist was applied. | Split source URL parsing from HTTP port validation so Bluesky/thread `at://...` identifiers pass only when explicitly allowed, while fetch targets remain strict HTTP/HTTPS. | Dev C could otherwise lose valid normalized Bluesky source documents that preserve AT URIs. | Added a regression for allowed AT URI metadata, unsupported AT URI metadata, and malformed AT URI metadata. | implemented |
| 2026-04-30 | Gate 5 Dev B metadata-safety review | Fresh C2 handoff review found arbitrary provider metadata strings were preserved without sanitization or prompt-injection scanning. | Sanitize nested string metadata values and include metadata text in document prompt-injection scans while preserving normal scalar metadata. | Dev C could otherwise receive malicious instructions in `ContextDocument.metadata` even when title/body were clean. | Added a metadata injection regression and reran focused retrieval/guardrail checks. | implemented |
| 2026-04-30 | Gate 5 Dev B score-surface review | Fresh consumer-contract review found public `Evidence.score` could expose out-of-range or infinite raw reranker values after correct raw-score ordering. | Preserve raw reranker ordering, then clamp/normalize public evidence scores into finite 0..1 values when building `Evidence`. | Dev C trust scoring, JSON traces, or eval artifacts could otherwise receive unstable score values. | Added a regression for `inf`, `nan`, and >1 reranker scores while preserving returned evidence order. | implemented |
| 2026-04-30 | Gate 5 Dev B metadata JSON-safety review | Fresh C2 serialization review found sanitized `ContextDocument.metadata` could retain non-finite floats in memory even though Pydantic JSON output masks them as null. | Normalize non-finite metadata floats to `None` during sanitization while preserving finite scalar metadata. | Dev C could consume typed retrieval objects before JSON serialization and encounter unstable `inf`/`nan` metadata values. | Added a sanitizer regression that checks in-memory metadata and JSON-mode output. | implemented |
| 2026-04-30 | Gate 5 Dev B provider-boundary review | Fresh end-to-end review found a provider could return a syntactically valid `SearchBundle` containing non-`ContextDocument` items, causing retrieval to crash later during source URL filtering. | Validate provider bundle document items at the search boundary, skip invalid items, and emit `search_provider_invalid_documents:*` warnings. | A malformed custom/offline provider could otherwise abort controlled Dev C handoff retrieval despite provider exception handling. | Added regression coverage by returning an invalid bundle item from a fake provider and reran focused/full review gates. | implemented |
| 2026-04-30 | Gate 5 Dev B RAG concurrency review | Fresh production-readiness review found concurrent calls on one `RagService` could interleave shared vector-store recreate/upsert/query state and return evidence from the wrong request. | Serialize the mutable RAG retrieval critical section with an instance lock while keeping search/fetch collection outside that lock. | Threaded API workers or future threadpool consumers could otherwise cross-contaminate C2 evidence and diagnostics. | Added a threaded vector-store overlap regression and reran focused/full review gates. | implemented |
| 2026-04-30 | Gate 5 Dev B provider cost/resource review | Fresh fifth-pass review found custom search providers could ignore `limit_per_provider` and return an unbounded number of valid documents for downstream embedding. | Enforced the provider result cap inside `collect_search_context()`, clamped negative limits to zero, and surfaced `search_provider_result_limit_exceeded:*` warnings for overflow. | A controlled provider or live wrapper could otherwise inflate retrieval cost and latency despite caller settings. | Added focused provider-limit regression coverage and reran focused/full review gates. | implemented |
| 2026-04-30 | Gate 5 Dev B sixth resource review | Fresh line-by-line review found direct provider/RAG negative limits, zero-limit RAG work, and arbitrary iterable metadata could bypass the normal bounded retrieval path. | Clamped direct built-in provider limits, query count, retrieval limit, and reranker limits to non-negative values, skipped embedding/vector work when retrieval or evidence limits are zero, and sanitized metadata iterables with bounded `islice` instead of materializing whole iterables. | Custom/offline providers could otherwise inflate cost, return surprising negative-slice results, call embedding providers despite zero result settings, or exhaust memory before metadata truncation. | Added direct provider-limit, query/RAG limit, zero-work, vector-store limit, and bounded metadata iterable regressions before rerunning full gates. | implemented |
| 2026-04-30 | Gate 5 Dev B seventh hardening review | Fresh component-boundary review found empty DNS resolver answers failed open, direct zero-limit ML rerankers still called models, and malformed provider warning strings split into character warnings. | Empty resolver answers now block with `dns_resolution_empty:*`, direct cross-encoder/DSPy rerankers return before model calls when `limit <= 0`, and provider string warnings normalize as a single warning. | A custom resolver or provider could otherwise weaken source-safety diagnostics or waste model calls outside the main retrieval path. | Added focused fetcher/search/reranker regressions and reran Dev B focused checks plus full deep review. | implemented |
| 2026-04-30 | Gate 5 Dev B eighth boundary review | Fresh end-to-end provider review found legacy `last_warnings` strings still split into characters and malformed DDGS hits could raise from the built-in web provider when called directly. | Normalized legacy string warnings as one warning and skipped malformed web-search hits with `web_search_invalid_hit:*` diagnostics. | Controlled/offline providers or changed DDGS result shapes could otherwise weaken diagnostics or crash direct provider use. | Added provider-boundary regressions and reran focused/full Dev B review gates. | implemented |
| 2026-04-30 | Gate 5 Dev B ninth provider-boundary review | Fresh line-by-line review found malformed provider warning containers such as `None` or scalar values could still discard otherwise valid provider documents or weaken diagnostics. | Added a shared warning normalizer for per-call and legacy providers so missing warnings become empty and scalar warnings become one diagnostic entry. | Controlled Dev C handoff providers could otherwise fail noisy while returning useful documents. | Added focused provider-boundary regression coverage and reran focused/full Dev B review gates. | implemented |
| 2026-04-30 | Gate 5 Dev B tenth fetch-boundary review | Fresh safe-fetch review found unexpected non-HTTP transport/client setup failures could still escape direct `LinkedPageFetcher.fetch()` instead of becoming diagnostics. | Converted unexpected fetch exceptions into `fetch_failed:<ExceptionType>` warnings while keeping URL blocking, HTTP errors, timeouts, and extraction failures specific. | A custom test transport or live runtime edge could otherwise abort linked-page retrieval despite safe input validation. | Added a focused fetcher regression and reran Dev B focused/full review gates. | implemented |
| 2026-04-30 | Gate 5 Dev B eleventh resource review | Fresh cost/control review found a zero search limit still called live-capable providers, and post links were deduped but not capped. | Zero `limit_per_provider` now skips provider calls entirely, built-in providers return before DDGS/Bluesky calls for non-positive limits, and `RetrievalSettings.linked_page_limit` caps linked-page fetches with `linked_page_limit_exceeded:*` diagnostics. | Controlled/offline callers could otherwise trigger no-result live searches or allow untrusted post context to drive unbounded outbound GET attempts. | Added provider zero-limit and linked-page cap regressions before rerunning focused/full gates. | implemented |
| 2026-04-30 | Gate 5 Dev B twelfth provider-iteration review | Fresh adapter-boundary review found DDGS results were sliced only after `list(ddgs.text(...))`, so a provider that ignored `max_results` could still be fully consumed. | Bounded DDGS result iteration with `islice` before materialization and kept Bluesky search result handling bounded to the caller limit. | A changed DDGS implementation or custom factory could otherwise inflate memory/time before retrieval caps ran. | Added an overconsumption regression that fails if DDGS is iterated past the caller limit. | implemented |
| 2026-04-30 | Gate 5 Dev B thirteenth provider-diagnostic review | Fresh warning-boundary review found mapping-shaped provider warnings were still treated as iterables and reduced to their keys. | Normalized mapping warnings as one diagnostic string while preserving valid list/tuple warning behavior. | A malformed custom provider could otherwise hide useful warning details even though retrieval continued safely. | Added a focused mapping-warning regression and reran focused/full Dev B review gates. | implemented |
| 2026-04-30 | Gate 5 Dev B upstream block-evidence review | Fresh C1/C2 handoff review found Dev A-originated `blocked_*` warnings were preserved as warnings but not promoted to private URL block evidence. | Seeded retrieval private blocks from startup and `PostContext.warnings` before collecting linked-page, search, and document URL block evidence. | Dev C trust/eval consumers could otherwise miss upstream private/local URL blocks even though the raw warning text was present. | Added an integration regression proving upstream block warnings populate `private_url_blocks`, diagnostics, and the `private_url_blocked` guardrail flag. | implemented |
| 2026-04-30 | Gate 5 Dev B DNS source-metadata review | Fresh provider-source review found DNS-backed private source URLs could pass metadata validation even though literal private URLs were blocked. | Made source metadata validation optionally resolver-aware and had retrieval resolve HTTP/HTTPS document source URLs before returning them in C2. | Custom or normalized providers could otherwise return citation/source URLs that resolve to private or local infrastructure with no `private_url_blocked` evidence. | Added a deterministic DNS-private provider regression that maps `127.0.0.1.nip.io` to loopback and verifies web, image, and normalized Bluesky source documents are blocked. | implemented |
| 2026-04-30 | Gate 5 Dev B invariant closure review | User requested moving from finding-by-finding fixes to invariant-driven Dev B closure. | Reused the resolver-aware document source URL policy in direct Bluesky providers, decoded bytes metadata before sanitization/scanning, and skipped optional reranker model calls for empty candidate lists. | Direct component paths could otherwise remain weaker than the final C2 boundary, and hidden metadata/model-cost edges would keep producing sibling findings. | Added source URL matrix coverage for direct Bluesky providers, bytes metadata prompt-injection scanning, empty-candidate reranker cost guards, DNS source-type matrix coverage, and a C2 invariant test for sanitized docs, resolver-safe source URLs, private block evidence, source-id consistency, finite scores, and JSON-stable metadata. | implemented |
| 2026-04-30 | Gate 5 Dev B C2 evidence-boundary review | Fresh invariant review found the final retrieval payload still trusted evidence objects returned by the RAG layer. | `make_retrieval_result()` now drops orphaned, duplicate, or empty evidence and clamps non-finite evidence scores before exporting C2 diagnostics. `collect_search_context()` also skips providers for blank queries. | Corrupted vector payloads or controlled custom RAG/search components could otherwise produce invalid source IDs, unstable scores, or no-result live/provider work despite a safe document boundary. | Added C2 boundary and blank-query provider regressions, then reran focused Dev B suites and full `make deep-review`. | implemented |
| 2026-04-30 | Gate 5 Dev B fourth invariant review | Fresh line-by-line boundary review found malformed constructed evidence/documents and shape-valid but semantically invalid DSPy ranked IDs could still crash or suppress valid fallback output. | Final C2 evidence normalization now rejects invalid items, accepts later valid duplicates after earlier invalid ones, and handles non-float scores as diagnostics. Document sanitization coerces malformed title/text/metadata objects as untrusted data, and DSPy reranking falls back when `ranked_ids` is not a list. | Custom/offline handoff components can use `model_construct()` or return malformed optional model output; those should degrade into warnings/fallbacks instead of violating C2 invariants. | Added C2 malformed evidence, malformed constructed document, and invalid DSPy ranked-ID regressions; focused Dev B suite passed before full review. | implemented |
| 2026-04-30 | Gate 5 Dev B fifth invariant review | Fresh boundary review found malformed constructed post links, malformed provider document fields, and bytes/string upstream warnings could still bypass the intended shared intake behavior. | Hardened dedupe and source-id hashing for malformed link values, coerced malformed `ContextDocument` id/source/title/url/text/metadata fields before sanitization, and normalized provider/startup/PostContext warnings before block-evidence promotion. | Custom/offline handoff objects constructed outside Pydantic validation could otherwise crash before URL blocking or hide private/local block evidence behind malformed warning containers. | Added C2 malformed document, malformed linked-post URL, bytes provider warning, bytes upstream warning, and string upstream warning regressions; focused Dev B matrix passed. | implemented |
| 2026-04-30 | Gate 5 Dev B sixth invariant review | Fresh C1 boundary review found malformed constructed `PostContext` text/thread/image fields and supplied query containers could still crash or split into characters before Dev B document intake. | Normalized post URL/AT URI/author/text/created_at, parent/quote/image containers, supplied query containers, and string/mapping link containers before document creation or link fetch. | Dev A/C controlled handoff objects built with `model_construct()` could otherwise bypass Pydantic and fail before source URL validation, sanitization, or block-evidence emission. | Added malformed post-context, malformed supplied-query, and string-link regressions; focused Dev B matrix passed. | implemented |
| 2026-04-30 | Gate 5 Dev B seventh C2 egress review | Fresh final-boundary review found custom RAG evidence text and non-string diagnostic flags could still enter the C2 payload after document intake was safe. | Routed final evidence through the shared C2 policy so evidence text is sanitized, scanned, source-validated, and score-normalized; normalized prompt flags, warnings, private block entries, and extra guardrail flags before building `RetrievalResult`. | Dev C trust/eval consumers could otherwise receive prompt-bearing evidence text or byte-valued guardrail diagnostics even when returned documents were safe. | Added C2 invariant coverage for malicious evidence text, byte diagnostic flags, warning normalization, prompt-injection guardrails, finite evidence scores, and JSON-stable evidence output. | implemented |
| 2026-04-30 | Gate 5 Dev B eighth invariant review | Fresh boundary probes found sanitized document IDs could collide after document intake and arbitrary post-link iterables were still consumed before the configured link cap. | Added final unique-ID normalization for sanitized documents in retrieval and direct RAG, bounded malformed link collection before fetch, and bounded/diagnosed malformed warning and C2 diagnostic iterables. | Dev C citations could otherwise receive ambiguous duplicate source IDs, or malformed Dev A handoff objects could still drive unbounded iteration before source-safety checks. | Added document-identity, bounded-link, warning-iterator, and diagnostic-iterator regressions; focused Dev B matrix passed. | implemented |
| 2026-04-30 | Gate 5 Dev B ninth identifier-boundary review | Fresh second-pass review found provider/RAG-controlled document and evidence IDs could carry prompt-injection text into returned C2 IDs or private URL block diagnostics. | Added shared safe identifier normalization for untrusted document/evidence labels while preserving established safe IDs such as `POST-target`, `LINK-*`, and `E*`. | Dev C prompts, citations, traces, or eval artifacts could otherwise see instruction-like source IDs even after title/text/metadata/evidence text were sanitized. | Added prompt-bearing document ID, blocked-document ID, and evidence ID regressions; focused guardrail/source/C2 checks passed. | implemented |
| 2026-04-30 | Gate 5 Dev B tenth vector-boundary review | Fresh direct-component probes found malformed in-memory embedding dimensions and malformed Qdrant point payloads could still raise below the final C2 safety layer. | Routed vector scoring and Qdrant point normalization through shared defensive payload helpers that clamp invalid scores, treat malformed vectors as zero similarity, and keep non-dict metadata JSON-stable. | Direct vector-store users or optional Qdrant payload drift could otherwise crash retrieval components before final C2 policy had a chance to normalize output. | Added vector-store payload regressions for dimension mismatch, malformed vector values, invalid point scores, and non-dict metadata before rerunning focused checks. | implemented |
| 2026-04-30 | Gate 5 Dev B eleventh handoff-boundary review | Fresh control-surface probes found raising parent/query/link/metadata/diagnostic iterables, bad string coercion, direct provider shape failures, and raising RAG diagnostics could still abort C2 before or after document/evidence safety policy. | Added shared bounded iterator and safe text helpers for malformed handoff containers, metadata, warnings, diagnostics, identifiers, and provider shapes, plus a guarded RAG diagnostics snapshot so these failures become targeted warnings or safe fallback text. | Dev A/C controlled handoff objects built with `model_construct()` or custom RAG/search implementations could otherwise crash the Dev B checkpoint despite safe document/evidence values. | Added matrix-style regressions for parent, quote, image, supplied query, link, link-cap, metadata, warning/diagnostic iterables, bad string coercion, direct provider result shapes, and RAG diagnostics failure cases; exact repro probes now degrade with warnings. | implemented |
| 2026-05-01 | Gate 5 Dev B twelfth safe-coercion review | Follow-up review found three remaining pre-sanitization `str(...)` crash paths: linked URL source-id hashing, provider document source-type filtering, and provider document ID dedupe. | Routed link source IDs, source-type allowlist checks, document dedupe keys, direct source metadata checks, and final evidence document-id sets through the shared safe text policy. | Custom Dev A/C handoff objects or providers could otherwise abort C2 before URL filtering, document sanitization, or evidence normalization. | Added integration regressions for bad-string link objects, provider document IDs, and provider source types; boundary grep, focused tests, and maintainability checks passed before full review. | implemented |
| 2026-05-01 | Gate 5 Dev B fifth invariant closure review | Final direct-component pass found sibling boundary gaps in optional reranker/query coercion, vector payload mappings, direct search queries, linked-page response/source-id coercion, and malformed resolver address objects. | Reused the shared safe text and payload policy across direct RAG, rerankers, search providers, Qdrant payloads, chunking, linked-page response normalization, and DNS address parsing. | Direct component use or custom test transports could otherwise continue producing the same crash/cost/control class even after the main C2 retrieval path was safe. | Added focused regressions for bad query text, bad candidate fields, bad chunk/document fields, bad Qdrant payloads, bad provider queries, bad fetch source IDs/peer hosts, and malformed resolver addresses. | implemented |
| 2026-05-01 | Gate 5 Dev B sixth invariant closure review | Final container-boundary review found malformed RAG evidence iterables, direct C2 query values, Sequence-shaped handoff containers, bad search-hit metadata mappings, provider containers, and direct RAG document containers could still bypass the invariant policy. | Bounded and normalized final evidence, C2 queries, Sequence handoff containers, search-hit metadata, provider lists, and direct RAG document inputs through the shared safety helpers. | Controlled Dev C/offline callers could otherwise still trigger the same boundary class through container shape rather than unsafe document text. | Added matrix regressions for malformed evidence/query/provider/document containers and search-hit metadata; focused Dev B matrix now covers 145 tests before full gates. | implemented |
| 2026-05-01 | Gate 5 Dev B missing-field closure review | Sixth deep review found a fresh sibling of the malformed-value class: `model_construct()` objects with missing post, document, or evidence fields could still raise before the shared intake policy. | Added shared safe field access and routed post context creation, document source filtering, direct provider URL warnings, document sanitization, final C2 evidence normalization, search-hit metadata, and direct chunk metadata through it. | Controlled Dev A/C handoff or offline provider/RAG objects could otherwise crash C2 by omitting fields rather than supplying malformed field values. | Added missing-field matrix regressions for post context, normalized provider documents, direct Bluesky provider URL blocking, and RAG evidence; focused Dev B matrix now covers 149 tests. | implemented |
| 2026-05-01 | Gate 5 Dev B seventh final boundary review | Final invariant pass found present-but-empty post/image source URLs from `model_construct()` could still fail during pre-filter `ContextDocument` construction. | Defaulted empty constructed post and image source URLs to `about:blank` so the resolver-aware source metadata policy blocks them and emits private URL evidence instead of crashing. | Controlled handoff objects could otherwise bypass the URL-block evidence path by supplying `None` instead of malformed strings. | Added a missing/empty URL C2 regression; focused Dev B matrix now covers 150 tests before full gates. | implemented |
| 2026-05-01 | Gate 5 Dev B final residual-boundary review | Fresh review of less-tested accessor and score coercion surfaces found hostile `created_at.isoformat`, RAG evidence score, and vector score objects could still raise from boundary conversions. | Guarded created-at ISO conversion, final C2 evidence score coercion, vector cosine coercion, and public vector score normalization so these values degrade to safe text or `0.0` diagnostics. | Controlled Dev A/C handoff objects or custom RAG/vector components could otherwise abort C2 after the document and evidence text policies had already succeeded. | Added residual-boundary regressions for created-at accessors, evidence score coercion, in-memory vector values, and Qdrant point scores; focused probes passed. | implemented |
| 2026-05-01 | Gate 5 Dev B numeric-control boundary review | Fresh cost-control review found hostile numeric limit objects could still raise in shared iterator bounds, query generation, search collection, rerankers, and vector queries. | Added a shared safe non-negative limit policy and routed C2 query, linked-page, provider, reranker, RAG, and vector-store limits through it. | Controlled Dev C/offline settings or direct component callers could otherwise abort retrieval before the safety policy produced bounded empty results. | Added regressions for bad limit objects across bounded dedupe, search collection, query generation, direct rerankers, RAG setup, and vector-store queries; exact hostile-limit probes now return empty safe results. | implemented |
| 2026-05-01 | Gate 5 Dev B sequence-cap boundary review | Fresh review of a not-yet-probed resource sibling found `Sequence` handoff containers were deduped before applying the configured cap. | Bounded sequence iteration before dedupe while preserving normal overflow counts for list-like containers. | Huge or hostile `Sequence` links/queries could otherwise consume work past the cap or discard valid in-cap items after an out-of-cap failure. | Added a regression proving a sequence that raises after the cap returns the first capped items and reports overflow without reading past the cap. | implemented |
| 2026-05-01 | Gate 5 Dev B huge-limit resource review | Fresh P2 resource review found valid but enormous numeric limits could still drive unbounded iteration/live-work budgets after bad-limit objects were handled. | Capped the shared safe limit policy at 200 and kept all query, iterator, search, linked-page, reranker, RAG, and vector-store limits routed through it. | Controlled settings or direct component calls could otherwise turn an infinite or oversized handoff container into excessive local work or live provider/fetch pressure. | Added a huge-limit regression and probe proving infinite iterables stop after 200 items while sequence overflow remains reported. | implemented |
| 2026-05-01 | Gate 5 Dev B final integration-usefulness review | Fresh direct-component probes found `RetrievalService` degraded malformed embedding output but direct `RagService.retrieve()` could still raise on empty embedding batches, malformed custom reranker output, malformed vector-store chunks, or hostile chunk fields. | Moved embedding, vector-store, reranker, and direct evidence construction behind a guarded RAG runtime helper that returns diagnostics or falls back to validated vector candidates instead of raising on malformed provider output. | Later integration or direct component callers could otherwise crash despite the final C2 wrapper being safe. | Added regressions for empty embedding batches, malformed reranker output, malformed vector-store chunks, and hostile chunk fields, then reran focused RAG, lint, and maintainability checks before the full review gates. | implemented |
| 2026-05-01 | Gate 5 Dev B final citation-invariant review | Fresh direct `RagService` review found custom vector-store chunks could produce evidence whose document/source IDs did not point to the current sanitized document set. | Direct RAG evidence construction now drops malformed or orphaned chunk candidates while preserving already-sanitized document IDs and emitting targeted diagnostics. | Later integration callers using `RagService` directly could otherwise consume uncited or stale evidence even though final C2 filtering would catch it. | Added direct RAG regressions for malformed chunk fields and orphaned vector-store chunks, then reran focused RAG, C2 bad-string, lint, and maintainability checks before full gates. | implemented |
| 2026-05-01 | Gate 5 Dev B consumer-contract strategy review | Final integration-readiness review found the hardened async C2 service did not yet have a tested adapter for Dev C's current synchronous `EvidenceRetriever` protocol. | Added `RetrievalEvidenceRetriever` as the consumer-contract adapter with sync and async full-result access, last-result diagnostics, guardrail/warning exposure, and fail-closed retrieval-error degradation. | C5 wiring could otherwise invent new glue and accidentally drop Dev B diagnostics, trace fields, or safe failure behavior at the handoff boundary. | Added integration coverage proving the adapter satisfies the agent retriever protocol, works inside a running event loop, exposes full C2 results asynchronously, and degrades unexpected service failures to warnings. | implemented |
| 2026-05-01 | Gate 5 Dev B artifact-contract strategy review | Final handoff review found consumers still needed to reassemble C2 JSON payloads by convention even though the Python object boundary was safe. | Added canonical retrieval payload helpers that serialize documents, evidence, diagnostics, warnings, guardrail flags, source IDs, scores, queries, and private block evidence into one JSON-stable shape. | Dev C, Dev D, and fixtures could otherwise drift in trace/eval serialization despite sharing the same safe retrieval result. | Added integration coverage that round-trips the canonical payload through JSON and validates the serialized document/evidence schema. | implemented |
| 2026-05-01 | Gate 5 Dev B invocation-state strategy review | Fresh consumer-contract concurrency review found a shared `RetrievalEvidenceRetriever` could expose another request's warnings or diagnostics through the mutable last-result accessor. | Scoped adapter result state with `ContextVar` while retaining last-result inspection fallback. | Threaded API workers or async tasks could otherwise misattribute source-safety warnings, guardrail flags, or diagnostics to the wrong post after retrieval succeeded. | Added a threaded adapter regression proving warning state remains isolated when one request reads diagnostics after another request completes. | implemented |
| 2026-05-01 | Gate 5 Dev B direct-provider state review | The same invocation-state strategy exposed a sibling in built-in search providers: direct `last_warnings` reads could be overwritten by another thread even though `collect_search_context()` was already per-call safe. | Backed built-in provider `last_warnings` with context/thread-local warning state while preserving iterable membership behavior for direct users and legacy collection. | Direct Dev B provider consumers could otherwise misattribute blocked URL or prompt-injection warnings under concurrent use. | Added a threaded direct-provider regression proving one provider call's warnings remain isolated after another call completes. | implemented |
| 2026-05-01 | Gate 5 Dev B diagnostic-accessor compatibility review | The invocation-state strategy needed to cover every post-work diagnostic accessor, not only adapters/providers, and stateful accessors still need to behave like the direct component contract they replaced. | Made built-in provider warning state list-like (`len`, indexing, equality, truthiness, repr), scoped direct `RagService.last_diagnostics` with context/thread-local state, and removed remaining avoidable raw string coercions in optional reranker/vector payload paths. | Threaded direct RAG consumers could otherwise see another invocation's prompt-injection diagnostics, while direct provider callers could break on the new stateful warnings object. | Added direct-provider list-like accessor coverage plus a threaded RAG diagnostics isolation regression; focused lint, mypy, and Dev B tests passed before final gates. | implemented |
| 2026-04-30 | Dev C Gate 5 C2/C3 scope | Dev C must prove consumption of Dev B-shaped retrieval output and expose a route-compatible explainer service without editing API routes, retrieval modules, schemas, eval, or frontend. | Added a Dev C evidence normalizer, provider-aware service loading, query-before-retrieval service flow, retrieval diagnostic trust/trace propagation, and a Gate 5 explainer fixture test. | C2/C3 fixtures could be mistaken for final C5 real Search/RAG acceptance, or missing optional providers could be treated as hard failures. | Handoff states C2/C3 are checkpoints only; optional providers skip with trace warnings, provider failures degrade to guarded fallback, and final route wiring/eval/frontend verification remain C5/Gate 6 work. | implemented |
| 2026-04-30 | Dev C Gate 5 review findings P1/P2 | Prompt-injection scanning must occur before query planning, and visible-post fallback bullets must cite the Bluesky post instead of a web evidence source. | Added pre-retrieval post/thread/image scanning to `AgentExplainerService`, skipped DSPy query generation with a trusted metadata query when that scan flags prompt injection, and added a stable post source (`S-post`) to response sources. | Risky post text could otherwise shape retriever queries, and fallback output could misattribute the visible post to unrelated web evidence. | Regression tests prove malicious visible-post text does not reach retriever queries and unknown-citation fallback cites `S-post`; handoff now documents the corrected C3 service behavior. | implemented |
| 2026-05-01 | Dev B C2 retrieval handoff | Dev C needs a consumer test against Dev B's canonical C2 retrieval payload without wiring `/api/explain`. | Added the Dev B C2 JSON fixture, hardened the agent evidence normalizer to accept JSON-stable document/evidence mappings and `private_url_blocks`, and added an integration test proving evidence sources become citations while prompt-injection/private-url diagnostics downgrade trust/fallback. | JSON payloads or duplicated private-url diagnostics could fail or over-penalize before Dev A's C5 route wiring. | Focused integration/unit checks cover the fixture, direct private-url block consumption, citation source coverage, warnings, guardrail flags, and trust downgrade. | implemented |
| 2026-05-01 | Gate 5 C5 integration branch | Dev A PR #5, Dev B PR #3, and Dev C PR #4 needed one route/dependency integration pass before final review. | Created standalone integration clone `/Users/akatsurada/rapidcanvas_c5_integration_isolated`, merged the three PR heads, resolved docs/log conflicts additively, wired `build_gate3_explainer()` to prefer Dev B `RetrievalEvidenceRetriever`, made the adapter query-aware for Dev C query plans, and added a C5 dependency-builder test. | Using Dev A's fallback wrapper after Dev B's adapter exists could drop adapter diagnostics/fail-closed behavior, while old Gate 4 tests could still assume thread-context fallback. | Full C5 checks pass with 271 backend tests, 8 frontend tests, `make deep-review`, and cached `make eval`; Dev E/D remain conditional recipients for UI and review/eval/matrix bookkeeping. | implemented |
| 2026-05-01 | Gate 5 C5 Dev E merge and Dev D final bookkeeping | PR #6 now includes Dev E PR #7, so docs and matrix needed to stop saying the route was still waiting for Gate 5 retrieval while avoiding Gate 6 eval claims. | Added the C5 checkpoint status doc, final review pointer, README/handoff refresh, and requirement-matrix closure for rows proven by C5 code/tests/review. | Stale docs could block PR readiness or overcorrect by marking public eval and quality rows implemented before Gate 6. | `R013`, `R014`, `R015`, `R024`, `R025`, `R034`, and `R045` now cite C5 proof; `R016`, `R017`, `R018`, and `R037` remain planned. | implemented |
| 2026-05-01 | Gate 5 C5 live-service limitation | Local no-key smoke returned only a `Thread` source and abstain fallback because provider/retrieval fallback paths fired. | Recorded the downgrade as trace-visible and kept public eval completion in Gate 6 instead of counting no-key fallback output as final acceptance. | Runtime provider skips or unauthenticated live-service limits could otherwise be mistaken for either a hard failure or a successful public eval. | Final review must use runtime-supplied keys for targeted real-service smokes; no generated live reports, Qdrant cache, `mlruns`, or secrets are tracked. | accepted |
| 2026-05-01 | Gate 6 Dev D isolated lane protocol | Dev D rapid delivery must run in a standalone clone, not the shared checkout. | Created `/Users/akatsurada/Documents/rapidcanvas_dev_d_gate6_isolated`, branch `codex/dev-d-gate6-eval-reports`, plus `assets/dev_D_gate6_WORKSPACE_CONTRACT.json` and wrapper scripts. | Shared checkout work could collide with other lanes or app-level git actions. | Ran the generic isolation verifier and execution-context guard before implementation. | implemented |
| 2026-05-01 | Gate 6 public eval coverage | Synthetic `example.com` URLs cannot count as final public Bluesky coverage. | Added 10 fixture-backed public Bluesky URLs verified by read-only AppView calls and marked all synthetic fixtures with `provenance=synthetic_fixture`. | Cached fixtures could be mistaken for live API refreshes or synthetic rows could inflate public coverage. | `make eval` reports public/synthetic counts, live/default mode status, and per-case provenance. | implemented |
| 2026-05-01 | Gate 6 optional judges | Ragas and DSPy judge should run only when stable, otherwise skip explicitly. | Default `make eval` remains deterministic/offline with skip reasons; explicit offline DSPy and Ragas judge commands ran after `make setup`. | Optional judge failures could block rapid delivery or be silently hidden. | Generated optional judge artifacts remain ignored under `reports/`; final review records exact command status. | implemented |
| 2026-05-01 | Gate 6 MLflow boundary | MLflow should not be hidden inside default eval because it creates local run artifacts. | Left MLflow behind explicit `make mlflow-log`, added skip/status text to default eval reports, then verified `make mlflow-log` explicitly. | Default eval could create `mlruns/` or imply tracking happened when it did not. | `.gitignore` keeps `mlruns/` ignored; latest local run `7a2d704cf7304735bb725ed3926b66e9` was generated locally, and `make deep-review` removes `backend/mlruns/`. | implemented |
| 2026-05-01 | Gate 6 requirement matrix honesty | Rows should close only when code, tests, eval artifacts, and docs exist. | Updated `R016`, `R017`, `R018`, `R019`, `R030`, `R031`, and `R037` with Gate 6 evidence while keeping image understanding and provider comparison reserved. | Premature implementation claims would overstate live provider quality or bonus features. | Matrix cells name cached/fixture-backed limits and point to Gate 6 final review/methodology docs. | implemented |
| 2026-05-01 | Gate 6 final evidence review | Manual line review found fixture/API-shape drift risk, broad prompt-injection leakage matching, and optional Ragas proxy ambiguity. | Restored `created_at` in public fixtures, narrowed injection leakage terms, counted explicit private URL block evidence, renamed public coverage to `public_bluesky_fixture_case_count`, labeled deterministic Ragas proxies, and added `test_gate6_eval_readiness.py`. | Reviewers could otherwise misread cached public coverage, confuse proxy metrics with Ragas execution, or miss fixture drift. | Readiness tests now check case mix, public/synthetic provenance, fixture/source shape, citation references, report artifacts, optional-tool labels, and summary truth fields. | implemented |
| 2026-05-01 | Gate 6 final unchecked-surface audit | Fresh review of API-mode eval adapters found local API-error rows had enough scoring fields but not the full trace-shape hints used elsewhere. | Added `trust_score=0.0` and `adapter_mode=api_eval_error` to eval API-error fixtures and covered it in the runner tests. | API-mode failure rows could otherwise look less trace-complete than cached fixtures during release-captain review. | Focused eval tests and final gates cover the corrected trace shape. | implemented |
| 2026-05-01 | Gate 6 final automated audit hardening | A fresh one-off QD audit checked manifest drift, attack fixture guardrail evidence, optional-tool labels, and Dev D ownership after the Dev A baseline. | Promoted manifest/public-case parity and prompt-injection/private-url/conservative-fallback evidence checks into `test_gate6_eval_readiness.py`; confirmed Dev D changes after `57aefac` stay inside allowed eval/docs/test/handoff paths. | Future edits could otherwise update `eval/posts.yaml` without updating the live manifest or weaken attack fixture evidence while still passing shape-only tests. | Readiness tests now fail on manifest drift, missing prompt-injection flags, missing private URL block evidence, or non-conservative attack fallbacks. | implemented |
| 2026-05-01 | Gate 6 explicit API-mode live-route smoke | The remaining unchecked eval surface was `--mode api`, which may perform live FastAPI/Bluesky reads and should not be conflated with cached evidence. | Ran `cd backend && uv run python -m app.eval.runner --mode api --judge deterministic --out reports/eval_api_final`; it completed without aborting, but local no-credential conditions produced `fallback_modes={"abstain": 19}` and lower live-route metrics. | API-mode success could otherwise be misread as final live quality closure even though every row abstained locally. | Final review, methodology, and handoff now record API-mode as a scored live-route limitation that release review must rerun with runtime credentials. | implemented |
| 2026-05-01 | Gate 6 prompt-injection raw fixture audit | The raw `eval/fixtures/prompt_injection/*` files existed but were not explicitly tied to the cached attack rows. | Added `eval/fixtures/prompt_injection/manifest.json`, linked web/Bluesky/image-alt/private-URL payloads to their eval cases, and made `test_gate6_eval_readiness.py` verify raw files, expected flags, and conservative fallbacks. | Reviewers or future maintainers could otherwise see prompt-injection files on disk without proof that the Gate 6 attack suite still maps to scored cases. | Requirement matrix, methodology, final review, and handoff now name the attack manifest and readiness coverage. | implemented |
| 2026-05-01 | Gate 6 final shipping audit | The last review needed an automated pass over surfaces that were easy to miss after the main QD work: baseline ancestry, forbidden path drift, fixture/report/doc consistency, and ignored generated artifacts. | Added `scripts/check_gate6_shipping_audit.py` and ran it after `make eval`; it checks Dev A baseline ancestry, Dev D ownership boundaries, 19-case dataset mix, public/synthetic manifest parity, raw attack-manifest evidence, report summary fields, final-review/methodology truth text, and ignored artifact rules. | A final handoff could otherwise rely only on normal tests and miss stale docs, changed report labels, or generated-artifact hygiene regressions. | Fixed the methodology wording so automated reviewers can detect the cached/offline claim, then reran the audit and required release gates. | implemented |
| 2026-05-01 | Gate 6 command-surface final review | A fresh last-pass inspection found the shipping audit existed but was not exposed through `make`, and README still carried stale pre-Gate-6 wording. | Added `make gate6-shipping-audit`, documented it in README and AGENTS, updated stale README handoff language, and extended the shipping audit to check command-surface and stale-Gate-6 text drift. | Reviewers could otherwise miss the final audit command or read README as saying public eval expansion was still future work after Dev D closed fixture-backed coverage. | The new make target regenerates eval reports and runs the audit; docs now point reviewers at the target. | implemented |
| 2026-05-01 | Gate 6 integration branch ownership | With no separate release captain, Dev D owns `gate6/integration` and must merge available Gate 6 lanes only after scope and smoke checks. | Created standalone integration clone `/Users/akatsurada/Documents/rapidcanvas_gate6_integration_isolated`, started `gate6/integration` from Dev A `57aefac`, then merged Dev D `d7d0da6`, Dev B `f3e1d2c`, Dev C `0664d07`, and Dev E `a969a59` after merge-base ownership checks. | Integrating from shared checkout or accepting broad cross-lane edits could overwrite another lane or hide drift. | All available lanes merged cleanly; focused lane smokes passed and final required Gate 6 commands are rerun from the integration branch. | implemented |
| 2026-05-01 | Gate 7 G7-C isolated lane protocol | Final truth/docs/submission work must not run from the shared checkout or alter G7-A/G7-B clones. | Created standalone clone `/Users/akatsurada/Documents/rapidcanvas_dev_g7c_final_truth`, branch `codex/g7-c-final-truth-docs-submission`, plus `assets/dev_G7_C_WORKSPACE_CONTRACT.json` and wrapper scripts. | Shared-checkout or cross-lane edits could overwrite runtime/optimization work. | Ran G7-C isolation verifier and execution-context guard before docs edits; G7-A/G7-B clones were read-only evidence sources. | implemented |
| 2026-05-01 | Gate 7 start condition | Gate 7 can only claim final acceptance after Gate 6 eval evidence lands. | Verified Gate 6 review exists, `make eval` passes with 19 cached cases, `public_bluesky_fixture_case_count=10`, `synthetic_fixture_case_count=9`, `make requirements-review` passes, and `make check-secrets` passes. | Proceeding without the Gate 6 truth layer would make final docs speculative. | Gate 7 final review records command evidence and distinguishes cached/public/synthetic coverage. | implemented |
| 2026-05-01 | Gate 7 Search/RAG and adaptive retrieval truth | Runtime Search/RAG must be separated from adaptive retrieval claims. | Documented the landed route as one-shot Dev B Search/RAG through `RetrievalEvidenceRetriever` with `ThreadContextEvidenceRetriever` as fallback/injected path; adaptive retrieval remains reserved. | Reviewers could otherwise read planned capped adaptive retrieval as shipped behavior. | README, requirement matrix, handoff, and final review name one-shot/non-adaptive status. | implemented |
| 2026-05-01 | Gate 7 G7-A handoff evidence | G7-A had local uncommitted runtime-finalization diffs, but they were not merged into the G7-C base. | Inspected G7-A diffs and recorded query-cap/full-diagnostic work as unlanded evidence only. | Counting unmerged local changes would overstate the submitted repository. | Final truth table counts only `origin/main` plus G7-C docs changes. | implemented |
| 2026-05-01 | Gate 7 GEPA dataset bridge and real compile status | GEPA should optimize over curated eval fixtures, but final docs cannot count unmerged G7-B code. | Documented that the landed `program.json` is dry-run metadata with `gepa_compile.executed=false`; no real compiled program or default loader-verified optimized program was produced. G7-B's eval-dataset bridge is noted as unmerged local diff. | Dry-run metadata could be mistaken for a real optimized DSPy program. | README, matrix, handoff, and final review classify GEPA as dry-run in the submitted base. | implemented |
| 2026-05-01 | Gate 7 image understanding truth | Image support must not imply live multimodal evaluation when only alt text/context evidence is landed. | Documented image URL/alt-text evidence as partial and live vision as skipped/reserved. | Reviewers could read image-context eval cases as proof of live vision. | Matrix `R032`, README bonus section, and final truth table separate alt-text fallback from live vision. | implemented |
| 2026-05-01 | Gate 7 provider comparison truth | Provider registry support is not the same as a live multi-provider benchmark. | Documented `GET /api/providers` and skipped-provider reasons as real registry behavior, while live Anthropic/Gemini/Ollama comparison remains skipped/config-limited. | Missing optional provider keys could masquerade as successful provider comparison. | Matrix `R033`, README, and final review state no live provider benchmark ran. | implemented |
| 2026-05-01 | Gate 7 MLflow/Ragas/judge status | Optional quality tooling must be separated from default deterministic eval. | Ran `make mlflow-log` locally and recorded it as file-backed local ops plumbing; left Ragas/DSPy provider-backed judges unrun in G7-C because the key was not present in the shell environment and default eval is no-network. | Generated `mlruns/` or optional judge results could be misread as committed or default evidence. | Generated artifacts remain ignored; final review records exact statuses and paths. | implemented |
| 2026-05-01 | Gate 7 pasted OpenAI key handling | The user pasted an OpenAI project key into chat and asked to use it for testing. | Did not write the chat key to `.env`, disk, command text, docs, or tracked files; continued with safe local/reproducible gates and recorded rotation guidance. | Putting the key into commands or files would create additional secret exposure. | `make check-secrets` passed; README/final review repeat the rotate-before-real-use guidance without including the secret. | accepted |
| 2026-05-01 | Gate 7 final truth automation gap | A final review document alone could drift after G7-C handoff, especially around dry-run, skipped, partial, and reserved claims. | Added `scripts/check_gate7_final_truth.py` and `make gate7-final-truth-audit` to verify truth-table classifications, eval counts, GEPA dry-run metadata, matrix status for R032/R033, allowed G7-C file scope, ignored artifacts, and no overclaim phrases. | Future docs edits could otherwise claim adaptive retrieval, live vision, live provider benchmarks, real GEPA compile, or provider-backed judges without evidence. | The audit runs after `make eval`, is documented in README/current handoff/final review, and fails on stale or flattering final-claim drift. | implemented |
| 2026-05-01 | Gate 7 final unchecked submission surfaces | A fresh G7-C pass line-read the requirement matrix and README after the audit was added. | Tightened matrix R008 so G7-C does not imply a fresh browser-use pass, tightened R039 so provider skip behavior does not point at an absent live provider report, and extended the Gate 7 audit to check runtime wiring evidence plus those matrix claims. | A reviewer could otherwise confuse prior smoke/browser checks or provider registry skips with new Gate 7 browser verification or a live provider benchmark artifact. | `make gate7-final-truth-audit` now checks the runtime retrieval adapter marker, thread-context fallback warning, browser verification wording, and no-live-provider-report wording. | implemented |
| 2026-05-01 | Gate 7 AGENTS handoff drift | Final truth review found `AGENTS.md` still stopped at Gate 6 and did not name the Gate 7 final truth audit. | Added a narrow Gate 7 G7-C status note, the `make gate7-final-truth-audit` command, and audit coverage for AGENTS wording. | Future agents could miss the final truth table or rely only on Gate 6 language. | `scripts/check_gate7_final_truth.py` now validates AGENTS includes the G7-C status and audit command without claiming live/adaptive/optimized behavior. | implemented |
| 2026-05-01 | Gate 7 push freshness | Final submission readiness should not rely only on a prose claim that the branch was pushed. | Extended `make gate7-final-truth-audit` to verify `origin/codex/g7-c-final-truth-docs-submission` exists and matches local `HEAD`. | A stale remote branch could otherwise leave the reviewer with older truth docs. | Final review and handoff now state branch freshness is mechanically checked. | implemented |
| 2026-05-01 | Gate 7 G7-B branch handoff | G7-B reported branch `codex/g7b-optimization-bonus` commit `3a79056` with real GEPA compile, dataset bridge, image helper, provider tests, MLflow metadata support, and passed final checks. | Fetched and inspected the remote branch, including changed files, `program.json`, dataset bridge, and delivery-review tests; documented it as ready-on-branch but not merged into the current G7-C branch. | Counting unmerged G7-B code as current submitted behavior would overstate this branch, while ignoring it would hide ready final integration evidence. | Final review, current handoff, README, and matrix now distinguish current G7-C dry-run/partial status from the G7-B branch that must be merged before claiming real compiled GEPA in the submitted base. | implemented |
| 2026-05-01 | Gate 7 dirty-tree audit gap | The remote freshness check could pass while tracked docs were locally modified but uncommitted. | Added clean tracked-tree checks to `make gate7-final-truth-audit` before branch freshness validation. | A final local edit could otherwise remain unpushed while the audit still reported remote freshness. | The final audit now fails on staged or unstaged tracked changes and must be rerun after commit/push. | implemented |
| 2026-05-01 | Gate 7 G7-B/G7-C integration | The previous G7-C final truth branch documented G7-B as ready but unmerged; the user asked whether G7-C and G7-B work together. | Created standalone integration branch `codex/g7bc-final-integration`, merged G7-C and G7-B cleanly, updated final truth docs and audit for the real compiled GEPA artifact plus G7-B image/provider tests. | Leaving docs in dry-run mode after merging G7-B would underclaim GEPA and confuse reviewers. | Final audit now checks real GEPA metadata, compiled artifacts, expanded Gate 7 allowed paths, clean tree, branch freshness, and continued skipped/partial caveats for provider, image UI, Ragas, and adaptive retrieval. | implemented |
| 2026-05-01 | Gate 7 B/C integration isolation | The integration pass also needed its own machine-checkable execution root, not only the earlier G7-C and G7-B lane roots. | Added `assets/dev_G7_BC_WORKSPACE_CONTRACT.json` plus wrapper scripts for the standalone integration clone and ran both verifier and context guard before final checks. | Without the B/C contract, final docs could truthfully be in a standalone clone but still miss the protocol's machine-checkable evidence. | The final review records the B/C verifier/context commands and the Gate 7 audit allows only the intentional B/C integration files. | implemented |
| 2026-05-01 | Gate 7 G7-A runtime merge | G7-A shipped branch `codex/g7-a-runtime-finalization` commit `fc4dff4` after the B/C final integration branch was already pushed. | Merged G7-A into `codex/g7bc-final-integration`, updated final truth docs and audit so Search/RAG remains real and adaptive retrieval is now a bounded real path instead of reserved. | Keeping old reserved wording would underclaim runtime behavior; claiming organic adaptive public coverage would overstate the handoff. | Final review records max-one-extra-query behavior, prompt-injection skip, forced live adaptive smoke, deterministic test evidence, and the caveat that natural public cases did not organically trigger round two before shipping. | implemented |
| 2026-05-01 | Gate 7 live public API usefulness smoke | Final assignment readiness needed a real-route check beyond cached fixtures. | Ran API mode over all 10 fixture-backed public Bluesky URLs and a direct 3-post TestClient sample. | No local provider key meant responses could be safe but not richly useful. | Final review and handoff now record that all 10 live public API-mode cases returned schema-valid cited 3-bullet `abstain` fallbacks; provider-backed live answer quality must be rerun with local credentials. | implemented |
