export default function App() {
  return (
    <main className="app-shell">
      <section className="workspace" aria-labelledby="app-title">
        <p className="eyebrow">T0 scaffold</p>
        <h1 id="app-title">Bluesky Contextual Post Explainer</h1>
        <p>
          The React shell is ready. The full URL input, provider selector,
          citations, trust display, and trace panel arrive in T12.
        </p>
      </section>
    </main>
  );
}

